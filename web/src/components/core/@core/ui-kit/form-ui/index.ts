export { setupForm } from './config';

export type {
  BaseFormComponentType,
  ExtendedFormApi,
  FormProps as BasicFormProps,
  FormSchema as BasicFormSchema,
} from './types';

export * from './use-form';
export * as z from 'zod';
