import BaseService from '@/api/core/base';
import type {{row_type_name}} from './model';

const baseUrl = '{base_url}';

export const {class_name}Service = {
  ...BaseService<{row_type_name}>({ baseUrl }),
};

export default {class_name}Service;
